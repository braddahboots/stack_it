function addText() {
  var inputText = document.getElementById('textBox').value;
  return ll.insert(inputText, 0);
}

function showText() {
  var show = '';
  ll.reset();
  while(ll.hasNext()) {
    str +=
  }
  document.getElementById('history').textContent = document.getElementById('history').innerHTML;
}